import { PlaceholderShell } from "@/components/shared/placeholder-shell";

export const metadata = {
  title: "Personas & Fit | Aexion Core",
};

export default function PersonasFitPage() {
  return <PlaceholderShell title="Personas & Fit" module="Data" />;
}
