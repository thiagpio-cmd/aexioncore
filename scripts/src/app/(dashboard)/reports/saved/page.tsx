import { PlaceholderShell } from "@/components/shared/placeholder-shell";

export const metadata = {
  title: "Saved Reports | Aexion Core",
};

export default function SavedReportsPage() {
  return <PlaceholderShell title="Saved Reports" module="Reports" />;
}
