import { PlaceholderShell } from "@/components/shared/placeholder-shell";

export const metadata = {
  title: "Pipelines & Stages | Aexion Core",
};

export default function PipelinesStagesPage() {
  return <PlaceholderShell title="Pipelines & Stages" module="Settings" />;
}
