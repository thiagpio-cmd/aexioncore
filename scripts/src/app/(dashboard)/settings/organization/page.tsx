import { PlaceholderShell } from "@/components/shared/placeholder-shell";

export const metadata = {
  title: "Organization | Aexion Core",
};

export default function OrganizationPage() {
  return <PlaceholderShell title="Organization" module="Settings" />;
}
