import { PlaceholderShell } from "@/components/shared/placeholder-shell";

export const metadata = {
  title: "Roles & Permissions | Aexion Core",
};

export default function RolesPermissionsPage() {
  return <PlaceholderShell title="Roles & Permissions" module="Settings" />;
}
