import { PlaceholderShell } from "@/components/shared/placeholder-shell";

export const metadata = {
  title: "Audit | Aexion Core",
};

export default function AuditPage() {
  return <PlaceholderShell title="Audit" module="Settings" />;
}
